<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}HPN!{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <body>
        <main role="main">
            {% block body %}{% endblock %}
        </main>
        

        <footer>
            <p>
                &copy; Copyright 2021 &middot;

                {% if app.request.attributes.get('_route') != 'app_about' %}
                 <a href="{{ path('app_about') }}">About us</a></p>
                 {% endif %}
            
        </footer>
    </body>
</html>
